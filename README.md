# Submission Approval System

## Features
- All submissions are saved as **pending**
- Admin can approve or reject submissions
- Public users only see approved submissions

## Setup

1. Copy .env.example to .env
2. Install dependencies:
   npm install
3. Run development server:
   npm run dev